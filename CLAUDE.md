# ส่วนจำของงานเรา

Claude อ่านไฟล์นี้ก่อนทุกครั้งที่เปิดโฟลเดอร์นี้ เราแก้ไฟล์นี้ได้เสมอ ตรงไหนไม่ใช่ ลบหรือเขียนใหม่ได้เลย

## กติกาข้อ 1: ถามก่อน ไม่เริ่มเอง

- ก่อนเริ่มทำงานชิ้นใหม่ (สรุป สไลด์ เอกสาร) ถามเราก่อนว่า งานนี้ให้ใครดู และต้องตอบอะไรบ้าง
- ถ้าเราแค่ถามเรื่องงาน ตอบได้เลยตามกติกาข้อ 3 ไม่ต้องถามกลับ
- ถามด้วยตัวเลือกให้กด (เครื่องมือถามคำถาม) ทีละข้อ และให้เราพิมพ์เองได้เสมอ
- ห้ามมีตัวเลือกที่เขียนว่า "แนะนำ" ห้ามเลือกแทนเรา
- ได้คำตอบแล้วค่อยเริ่ม
- งานเสร็จแล้ว ห้ามบอกเองว่างานผ่าน คนตรวจคือเรา

## กติกาข้อ 2: ชื่อจริงไม่ขึ้นลิงก์

- ลิงก์ Artifact บนแพ็กเกจ Pro เป็นลิงก์สาธารณะ ใครได้ลิงก์ก็เปิดดูได้
- ก่อนสร้างลิงก์ ตัดชื่อจริง เบอร์โทร และข้อมูลลูกค้าจริงออกทุกครั้ง
- ไม่แน่ใจว่าอะไรเป็นข้อมูลจริง ถามเราก่อน

## กติกาข้อ 3: ตอบจากส่วนจำ ไม่เดา

- เวลาเราถามเรื่องงาน ตอบจากไฟล์ในโฟลเดอร์นี้ก่อน และบอกว่าอ่านมาจากไฟล์ไหน
- ไม่มีในไฟล์ บอกตรง ๆ ว่าไม่มี อย่าเติมเอง

## ในโฟลเดอร์นี้มีอะไร

| ที่ | มีอะไร |
|---|---|
| `core/` | ของที่ใช้ทุกงาน: ทีมเราเป็นใคร ใครทำอะไร น้ำเสียงของเรา |
| `users/` | คนที่เราทำงานให้ เขาอยากได้อะไร |
| `decision-log.md` | สิ่งที่ตัดสินใจไปแล้ว และเพราะอะไร (ใหม่สุดอยู่บน) |
| `meetings/` | สรุปประชุม skill `/meeting-slide` เขียนไว้ที่นี่ |
| `slides/` | สไลด์ที่ skill `/meeting-slide` ทำ (มีหลังใช้ครั้งแรก) |
| `templates/slides.html` | Template สไลด์ |
| `sample/meeting-transcript.md` | transcript ประชุม 35 นาที ตัวอย่างสมมติ ไว้ฝึก |
| `.claude/skills/` | วิธีทำงานที่เก็บไว้ใช้ซ้ำ: `/meeting-slide` transcript ประชุม เป็นสไลด์สรุปพร้อมลิงก์ |
| `docs/` | คู่มือติดตั้ง Claude Code · ส่วนจำ · Connectors สำหรับคน ไม่ต้องแก้ |

## ถ้าเพิ่งติดตั้งเสร็จ

บอกเราว่า "ติดตั้งเสร็จแล้ว เปิด session ใหม่ในโฟลเดอร์เดิม แล้วลองถามว่า งานนี้มีใครบ้าง ตัดสินใจอะไรไปแล้ว"
(เปิด session ใหม่แล้ว Claude ถึงเห็น skill ครบ)

## ข้อมูลในโฟลเดอร์นี้

ทีม คน และประชุมในไฟล์ตั้งต้นเป็น **ตัวอย่างสมมติ** ทั้งหมด ไม่ใช่คนหรือทีมจริง
พอใช้กับงานของเราเอง แก้ `core/` `users/` และ `decision-log.md` เป็นของเราได้เลย
